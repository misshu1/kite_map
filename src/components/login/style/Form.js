import styled from "styled-components";

export const Form = styled.form`
    p {
        margin: 0.5rem 0;
    }
    @media (min-width: 15rem) {
        && {
            width: 15rem;
        }
    }
`;
